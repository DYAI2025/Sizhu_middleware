node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Local PRIL enforcement shims (agileteam infra; resolves repo-relative hook path to the global install)
config/claude/

# Local Claude Code agent state (homunculus observer etc.) — never product content
.claude/

# Stryker mutation report output (transient)
reports/mutation/
.stryker-tmp/

# Font binaries (AM-4 CJK render spike) — large, redistributable under OFL but not
# committed; commit only assets/fonts/OFL.txt + the README note. Fetched by the
# spike per scripts/smoke/cjk-render-spike.ts header.
assets/fonts/*.ttf
assets/fonts/*.otf
